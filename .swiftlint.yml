# Swift Lint Fucking Stuff

excluded: # paths to ignore during linting. Takes precedence over `included`.
- Pods
- ../Pods
- ./Pods
- /Pods
- Project/R.generated.swift
- Project/Pods

disabled_rules: # rule identifiers to exclude from running
- trailing_whitespace
- line_length
- vertical_whitespace
- trailing_newline

